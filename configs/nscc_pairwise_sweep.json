{
  "max_experiments": 4,
  "walltime_hours": 2.0,
  "convergence_epsilon": 0.0001,
  "convergence_patience": 3,
  "max_retries": 2,
  "log_dir": "artifacts/pairwise_sweep",
  "decision_mode": "queued",
  "candidates": [
    {
      "name": "dcn_bpr_lr1e3_pairs2",
      "hypothesis": "BPR directly optimizing clicked-over-non-clicked order should align DCN training with NDCG better than pointwise BCE.",
      "config": "configs/nscc_dcn_features.json",
      "overrides": ["training.batch_size=1024", "training.loss_name=bpr", "training.pairwise_negatives=2", "training.epochs=60", "training.patience=8"]
    },
    {
      "name": "dcn_bpr_lr5e4_pairs2",
      "hypothesis": "A lower BPR learning rate may delay the rapid ranking overfit observed in smoke testing.",
      "config": "configs/nscc_dcn_features.json",
      "overrides": ["training.batch_size=1024", "training.learning_rate=0.0005", "training.loss_name=bpr", "training.pairwise_negatives=2", "training.epochs=60", "training.patience=8", "training.lr_scheduler_factor=0.5", "training.lr_scheduler_patience=2"]
    },
    {
      "name": "dcn_bpr_lr5e4_pairs4",
      "hypothesis": "More within-user logged negative pairs may give a stronger ranking gradient.",
      "config": "configs/nscc_dcn_features.json",
      "overrides": ["training.batch_size=1024", "training.learning_rate=0.0005", "training.loss_name=bpr", "training.pairwise_negatives=4", "training.epochs=60", "training.patience=8", "training.lr_scheduler_factor=0.5"]
    },
    {
      "name": "dcn_hybrid_rank_calibration",
      "hypothesis": "A mostly pairwise objective with a small BCE term may preserve calibrated click scores while improving order.",
      "config": "configs/nscc_dcn_features.json",
      "overrides": ["training.batch_size=1024", "training.learning_rate=0.0005", "training.loss_name=hybrid", "training.hybrid_bce_weight=0.25", "training.pairwise_negatives=2", "training.epochs=60", "training.patience=8", "training.lr_scheduler_factor=0.5"]
    }
  ]
}
