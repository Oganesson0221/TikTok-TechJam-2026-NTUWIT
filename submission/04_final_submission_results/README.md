# Final submission and results

Upload `files/kuairand_pure.csv` for the required benchmark. It has the exact Starter Kit schema and was generated without reading test outcomes. The seven Pure component checkpoints plus `ensemble.json` reproduce its seed-averaged scoring model. The completed 1K bonus output/checkpoint is included. `checkpoints/openai_agent_best.pt` and the logs folder preserve the completed six-decision GPT-5 mini autonomous run; its weaker checkpoint did not replace the validation-best ensemble.
